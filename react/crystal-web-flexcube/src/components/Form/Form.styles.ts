import { space } from "../../styles/common.style";

export const style = `
    width: 100%;
    > .collapse { 
        margin-bottom: ${space};
    }
    &-content {
        padding: ${space};
    }
`;

export const filedStyle = `
    padding: ${space};
    align-items: center;
`;
